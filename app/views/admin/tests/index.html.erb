<h1><%= t('.header') %></h1>

<table>
  <tr>
    <th><%= t('helpers.label.all_tests.id') %></th>
    <th><%= t('helpers.label.all_tests.title') %></th>
    <th><%= t('helpers.label.all_tests.level') %></th>
    <th><%= t('helpers.label.all_tests.author') %></th>
    <th><%= t('helpers.label.all_tests.created_at') %></th>
    <th colspan="3"><%= t('helpers.label.all_tests.action') %></th>
  </tr>
  <%= render @tests%>
</table>
<p>
<%= link_to 'All users', admin_users_path %> |
<%= link_to 'All questions', admin_questions_path %> |
<%= link_to 'All answers', admin_answers_path %> |
<%= link_to 'Create new test', new_admin_test_path(@test) %>
</p>